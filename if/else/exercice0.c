#include <stdio.h>

int main() {
    
    int x = 5;


    if (x > 0) {
        printf("La valeur de x est positif : %d\n", x);
    }
    else {
        printf("La valeur de x est negatif ou nulle : %d\n", x);
    }
    
    return 0;

}